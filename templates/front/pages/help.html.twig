{% extends 'layout/base.html.twig' %}

{% block page_title %} Помощь {% endblock %}

{% block content %}
    <div class="col">
        <div class="card">
            <div class="card-body">
                <h4 class="header-title">{{ page.title }}</h4>
                <div>
                    {{ page.text|raw }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
