{% extends 'layout/base.html.twig' %}

{% block page_title %} Здравствуйте, {{ app.user.fullName }} {% endblock %}

{% block content %}
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="header-title mb-4">Настройки пользователя</h4>

                <ul class="nav nav-pills navtab-bg nav-justified">
                    <li class="nav-item">
                        <a href="{{ path('profile') }}" class="nav-link">
                            Общая информация
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('user_progress') }}" class="nav-link">
                            Обучение
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('user_settings') }}" class="nav-link active">
                            Настройки
                        </a>
                    </li>
                </ul>
                <div class="tab-content">
                    <div class="tab-pane show active" id="home1">
                        {{ form(profileForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
